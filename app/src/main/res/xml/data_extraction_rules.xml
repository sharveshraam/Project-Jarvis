<?xml version="1.0" encoding="utf-8"?>
<!--
  Android 12+ data extraction rules (backup and device-to-device transfer).

  Same policy as backup_rules.xml: no Jarvis data leaves the device implicitly.
  `disableIfNoEncryptionCapabilities` additionally refuses cloud backup on accounts
  that cannot encrypt it.
-->
<data-extraction-rules>
    <cloud-backup disableIfNoEncryptionCapabilities="true">
        <exclude domain="database" path="." />
        <exclude domain="sharedpref" path="." />
        <exclude domain="file" path="." />
        <exclude domain="external" path="." />
        <exclude domain="root" path="." />
    </cloud-backup>
    <device-transfer>
        <exclude domain="database" path="." />
        <exclude domain="sharedpref" path="." />
        <exclude domain="file" path="." />
        <exclude domain="external" path="." />
    </device-transfer>
</data-extraction-rules>
